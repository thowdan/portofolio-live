{
  "name": "thowdan-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal portfolio with a server-rendered homepage and a backed admin editor, deployed on Vercel.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "preview": "node scripts/preview.js",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "three": "^0.185.1"
  }
}
